{
    "name": "SecureGate",
    "root": "Backends/SecureGate",
    "sourceRoot": "Backends/SecureGate/src",
    "targets": {
        "build": {
            "executor": "@nx-dotnet/core:build",
            "options": {
                "configuration": "Production"
            }
        },
        "watch": {
            "executor": "@nx/workspace:run-commands",
            "options": {
                "command": "dotnet watch --project Backends/SecureGate/SecureGate.csproj"
            }
        },
        "test": {
            "executor": "@nx-dotnet/core:test",
            "options": {}
        }
    }
}